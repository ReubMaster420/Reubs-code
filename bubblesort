Array = [3 , 2 , 5 ,10,  3 , 50 , 4 , 8 , 6,] 
while True:
    for i in range(len(Array)-1):
        if Array[i] > Array[i+1]:
            temp = Array[i]
            nexto = Array[i+1]
            Array[i] = nexto
            Array[i+1] = temp
            print (Array)
